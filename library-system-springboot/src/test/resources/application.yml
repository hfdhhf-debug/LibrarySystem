spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  borrow:
    defaultDays: 30
  jwt:
    secret: "CHANGE_ME_TO_A_LONG_RANDOM_SECRET_>=_32_CHARS"
    expireMinutes: 60
